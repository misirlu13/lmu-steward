# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed

# Coverage directory used by tools like istanbul
coverage
.eslintcache

# Dependency directory
# https://www.npmjs.org/doc/misc/npm-faq.html#should-i-check-my-node_modules-folder-into-git
node_modules

# OSX
.DS_Store

release/app/dist
release/build
.erb/dll

.idea
npm-debug.log.*
*.css.d.ts
*.sass.d.ts
*.scss.d.ts

/fixture-test-set

# Live capture sidecar build output. The binary DOES ship — electron-builder
# copies it in as resources/lmu-spike.exe — but it is built from source during
# packaging, so the artifact itself stays out of git.
tools/live-capture-spike/build

# Working design and implementation plans. Local-only so they survive branch
# switches and never reach origin.
/plans
